import React from 'react';

const Home: React.FC<{}> = () => (
	<div style={{ marginLeft: '45%', marginTop: '10%', width: '25%' }}>
		<h2> Hey there, this page has no real usage for now :( </h2>
	</div>
);

export default Home;
